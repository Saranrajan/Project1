name: Build Hangover ARM64 Wine Runtime

on:
  push:
    branches:
      - 'feature/*'
      - 'feature/**'
      - 'feature/native-arm64-runtime'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build-arm64-wine:
    name: Build Hangover ARM64 Wine runtime
    runs-on: ubuntu-24.04-arm
    steps:
      - name: Checkout experiment repository
        uses: actions/checkout@v4

      - name: Clone Hangover and initialize its runtime submodules
        shell: bash
        run: |
          set -euo pipefail
          rm -rf third_party/hangover
          git clone --depth 1 --branch master https://github.com/AndreRH/hangover.git third_party/hangover
          git -C third_party/hangover submodule update --init --recursive --depth 1

      - name: Install Wine build dependencies
        shell: bash
        run: |
          set -euo pipefail
          sudo sed -i 's/^Types: deb$/Types: deb deb-src/' /etc/apt/sources.list.d/ubuntu.sources || true
          sudo apt-get update
          sudo apt-get install -y \
            build-essential clang lld flex bison gettext pkg-config python3 curl jq unzip xz-utils zstd dpkg-dev fakeroot
          sudo apt-get build-dep -y wine || true

      - name: Download Linux ARM64 LLVM-MinGW cross toolchain
        shell: bash
        run: |
          set -euo pipefail
          llvm_mingw_version=20260616
          llvm_mingw_host=ubuntu-22.04-aarch64
          archive="llvm-mingw-${llvm_mingw_version}-ucrt-${llvm_mingw_host}.tar.xz"
          asset_url="https://github.com/mstorsjo/llvm-mingw/releases/download/${llvm_mingw_version}/${archive}"
          mkdir -p toolchains
          curl -fL --retry 3 "${asset_url}" -o "toolchains/${archive}"
          tar -xJf "toolchains/${archive}" -C toolchains
          llvm_root="$PWD/toolchains/llvm-mingw-${llvm_mingw_version}-ucrt-${llvm_mingw_host}"
          test -d "${llvm_root}/bin"
          test -x "${llvm_root}/bin/aarch64-w64-mingw32-clang"
          test -x "${llvm_root}/bin/arm64ec-w64-mingw32-clang"
          test -x "${llvm_root}/bin/i686-w64-mingw32-clang"
          echo "LLVM_MINGW_ROOT=${llvm_root}" >> "$GITHUB_ENV"
          echo "${llvm_root}/bin" >> "$GITHUB_PATH"

      - name: Verify Hangover Wine source
        shell: bash
        run: |
          set -euo pipefail
          test -x third_party/hangover/wine/configure
          test -d third_party/hangover/fex
          test -d third_party/hangover/box64

      - name: Apply upstream ARM64EC fixes
        shell: bash
        run: |
          set -euo pipefail
          python3 scripts/patch_hangover.py third_party/hangover/wine

      - name: Configure Hangover Wine
        working-directory: third_party/hangover/wine
        run: |
          set -euo pipefail
          command -v mkdir
          command -v clang
          echo "PATH=${PATH}"
          mkdir -p build
          cd build
          ../configure \
            --disable-tests \
            --with-mingw=llvm-mingw \
            --enable-archs=arm64ec,aarch64,i386

      - name: Build Hangover Wine
        working-directory: third_party/hangover/wine/build
        shell: bash
        run: |
          set -euo pipefail
          set +e
          make -j"$(nproc)" 2>&1 | tee build.log
          make_status=${PIPESTATUS[0]}
          set -e
          if (( make_status != 0 )); then
            failure_summary="$(grep -Eim1 '(^|[^[:alnum:]_])(fatal )?error:|^make(\[[0-9]+\])?: \*\*\*' build.log || tail -n1 build.log)"
            failure_summary="${failure_summary//'%'/'%25'}"
            failure_summary="${failure_summary//$'\r'/'%0D'}"
            failure_summary="${failure_summary//$'\n'/'%0A'}"
            echo "::error title=Hangover Wine build failure::${failure_summary}"
            exit "${make_status}"
          fi

      - name: Install runtime into staging root
        working-directory: third_party/hangover/wine/build
        run: |
          set -euo pipefail
          rm -rf "$GITHUB_WORKSPACE/artifacts/rootfs"
          mkdir -p "$GITHUB_WORKSPACE/artifacts/rootfs"
          make DESTDIR="$GITHUB_WORKSPACE/artifacts/rootfs" install

      - name: Package runtime
        run: |
          set -euo pipefail
          mkdir -p artifacts
          tar --zstd -C artifacts/rootfs -cf artifacts/hangover-arm64-wine-root.tar.zst .
          sha256sum artifacts/hangover-arm64-wine-root.tar.zst > artifacts/hangover-arm64-wine-root.sha256

      - name: Upload ARM64 Wine runtime
        uses: actions/upload-artifact@v4
        with:
          name: hangover-arm64-wine-runtime
          path: |
            artifacts/hangover-arm64-wine-root.tar.zst
            artifacts/hangover-arm64-wine-root.sha256
          retention-days: 14
